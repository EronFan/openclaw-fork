# 2026-04-03 日记

> 创建时间：2026-04-03 21:27 CST
> 记录者：main agent cron

## 今日概要

（由 cron 21:27 自动创建，待今日活动填充）

---

## 昨日任务汇总（2026-04-03）

> 由 cron 2026-04-04 00:00 整理写入

### ✅ 完成事项

1. **晨报已发送**（09:08 CST，飞书群）：覆盖 Feishu v2026.4.1 regression（P1）、exec 审批策略收紧（P1）、InStreet 站点装修（24h+）
2. **InStreet 学习笔记整理完成**（~17:00 CST）：记录 Instance 身份哲学、文学社每日一作限制、PlayLab 桌游实验室、预言机预测市场等社区观察
3. **workspace 晚间收口 cron 完成**：新增 `.gitignore` 排除 `jobs.json` 和 `scripts/__pycache__`，保持 workspace 整洁
4. **OpenClaw 反馈检查 cron 完成**：确认 #55008 maintainer 已完成修复并确认合并；其余 P1/P2 继续等反馈
5. **项目进展日报已发送**（飞书群，~21:35 CST）：列出 #60314（P1）、#60309（P1）、#60312（P1）三个新 issue
6. **新模型注册**：minimax provider 新增 `MiniMax-M2.7-highspeed`，同步写入 `agents.defaults.models`（SSOT 规则），Gateway 重启生效
7. **Gateway 故障修复**：检测到 RPC 层 `pairing required` 问题导致 spawn/cron 均失效，已执行 gateway 重启修复

### 🔄 进行中 / 阻塞事项

1. **xixi → main 回传链路超时**：sessions_send 持续报 gateway timeout（1008 pairing required），链路拓扑待优化
2. **OpenClaw 反馈检查 rate limit**：连续 8+ 次 429 错误，GLM-4.7/glm-5.1 周配额已耗尽，预计 4 月 6 日重置
3. **aoao 转发超时**：sessions_send 到 aoao 的直达链路也受影响（pairing required），Gateway 重启后待验证
4. **exec 审批策略收紧**：gh CLI、find 等命令在多个 cron 中被拦截；weiwei 会话确认需手动审批（安全策略变更后效应）

### 🎯 关键决策 / 结论

1. **新模型 `MiniMax-M2.7-highspeed` 已正式注册**，按 SSOT 规则同步到 agents.defaults，重启后生效
2. **Gateway RPC pairing 问题已修复**（重启），后续 spawn 和 cron 链路应恢复正常
3. **InStreet 站点装修中**（闭店超过 24h），社区内容暂时不可抓取，学习笔记基于本地沉淀保守整理
4. **#55008 skills regression 已由 maintainer 确认修复完成**，即将合并，无需再跟

### 📣 晨报可引用 bullet（3-8 条）

- 🔴 **Telegram DM pairing sessions 存在 exec approval 绕过**（#60314，P1安全）：Telegram DM 场景下 exec 安全检查被绕过，建议优先评估修复
- 🔴 **v2026.4.2 缺少 grammy 依赖**（#60309）：macOS 用户启动即崩，需发版修复或回退
- 🔴 **Gateway pairing 问题已修复**：RPC 层 pairing required 导致 spawn/cron 失效，重启 Gateway 后恢复正常
- 🟡 **xixi 新发现 7 个 GitHub 候选**：包括 web_fetch Fake-IP 阻塞（#60393）、Feishu 解析缺口（#60380）、Telegram retry 循环（#60362）等
- ✅ **#55008 skills regression 已由 maintainer 确认修复**，即将合并
- ⚠️ **GLM 模型配额耗尽**：OpenClaw 反馈检查链路上周 GLM-4.7/glm-5.1 周配额用尽（8 次 429），预计 4 月 6 日恢复，可切换其他模型规避
- 📝 **InStreet 站点装修中**：社区热帖暂时不可见，学习笔记基于文档积累整理；Instance 身份哲学、AI 桌游推理等主题值得持续关注
