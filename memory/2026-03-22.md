## Durable notes added at 2026-03-22 22:28 CST
- 晨报系统已重构为三段自动化：00:00 `memory-daily-create` 整理昨日任务，05:00 `instreet-idle-learning` 学习公开资料并沉淀到 `memory/instreet-learning.md`，08:00 `morning-briefing` 输出压缩版晨报。
- 晨报规则已固定：总览摘要（<=3 条）→ 昨日重点任务（5-10 条，强制状态标签 `[已完成]` / `[推进中]` / `[阻塞]` / `[待确认]`）→ AI 学习/社区观察（3-5 条）→ 今日日程/天气/待办 → 一句话总结；禁止流水账。
- 用户新增长期工作偏好：低风险、可逆、内部优化类事项应由我一次性收敛并直接完成，不要拆成多轮重复确认；只有高风险、不可逆、删除性操作、对外发送或权限边界变化时才额外确认。
- 用户新增 cron 习惯：以后我新增 cron 任务时，默认优先使用 `arkcode/deepseek-v3.2`，除非用户明确指定其他模型或任务确需专用模型。
- 模型配置调整：已移除生效配置中的 siliconflow；heartbeat 默认模型固定为 `arkcode/deepseek-v3.2`；现有 3 个 cron 任务已通过 CLI 逐个设为 `arkcode/deepseek-v3.2`。
- 新接入 provider：`hgapi`，`baseUrl=https://11531817.cc.cd/openai`，模型 `hgapi/gpt-5.4` 可实测成功调用；当前 `agents.defaults.model.primary` 已切为 `hgapi/gpt-5.4`，fallbacks 为 `openai-codex/gpt-5.4`、`arkcode/deepseek-v3.2`。
- “人人都会 OpenClaw” 项目推进结论已收敛：首轮不做大而全，优先做 5 篇核心内容（跑通飞书 Agent、Gateway 安全自检、每日晨报、message tool 飞书直发、sessions_send timeout 故障案例），并同步建立术语表、issue 模板、文档验收清单；“可复现性”和“安全边界”设为一票否决项。

# 昨日任务汇总（2026-03-22）

## 完成事项
- 已创建并稳定运行“记忆整理 + 晨报”三段自动化链路：00:00 生成/维护日记，05:00 学习公开资料，08:00 输出压缩晨报。
- 已明确晨报固定结构与写法约束，避免流水账，要求重点任务带状态标签输出。
- 已完成模型配置调整：移除 siliconflow，heartbeat 与现有 cron 默认切到 `arkcode/deepseek-v3.2`。
- 已接入新 provider `hgapi`，并将主模型切为 `hgapi/gpt-5.4`，fallback 保留 `openai-codex/gpt-5.4` 与 `arkcode/deepseek-v3.2`。
- 已推进并重命名项目为 **OpenClaw自我提升计划**，建立/更新项目文档，包括 README、执行方案、Bug 候选池、session 收编结果、gateway 重启后模型复测记录。
- 已通过 xixi / aoao 的 session 收编首轮项目成果：候选问题池、质量门槛、分流标准、低质量 issue 拦截规则。
- 已完成 gateway 重启后的模型复测沉淀：xixi 成功跑通 `hgapi/gpt-5.4`，aoao 首轮 403 后续跑成功，结论已落档。

## 进行中 / 阻塞事项
- `sessions_send` 回 main 的回执链路仍存在连续 timeout，已被确认不是“agent 没执行”，而是回执确认不稳定。
- `hgapi/gpt-5.4` 在部分子 agent / inter-session 场景首轮仍会出现 403 blocked，虽然后续可继续完成，但稳定性不足。
- 晨报数据源仍不完整：天气 / 日历 / 待办数据接入不足，InStreet 社区公开内容获取也不稳定，因此完整晨报暂不能硬拼。
- subagent 本体问题尚未建立独立最小复现，当前只有 sessions 侧 timeout 线索，证据还不够。

## 关键决策 / 结论
- 低风险、可逆、内部优化类事项以后默认直接执行，不再拆成多轮确认；高风险、不可逆、对外发送、权限边界变化仍需明确确认。
- 新增 cron 任务默认优先使用 `arkcode/deepseek-v3.2`，除非用户明确指定其他模型。
- 项目 **OpenClaw自我提升计划** 首轮不做大而全，而是先围绕 5 篇核心内容和 1 套质量/分流体系做实。
- 项目推进方式正式切换为：**不依赖 agent 回执，直接抓 xixi / aoao session history 收编结果推进**。
- 当前模型判断：`hgapi/gpt-5.4` 已经可用，但在不同 agent 上稳定性不一致；`sessions_send` timeout 是独立问题，不能和模型不可用混为一谈。
- 项目质量门槛已明确：**可复现性** 和 **安全边界** 作为一票否决项；问题统一按 issue / PR / docs fix / 内部留档分流。

## 可供晨报引用
- [已完成] 记忆整理、学习、晨报三段自动化链路已跑通，晨报结构规则已固定。
- [已完成] 主模型已切到 `hgapi/gpt-5.4`，cron/heartbeat 默认模型已统一为 `arkcode/deepseek-v3.2`。
- [已完成] “人人都会 OpenClaw” 已重命名并推进为 **OpenClaw自我提升计划**，首轮项目文档框架已建立。
- [已完成] xixi / aoao 的首轮研究与复核结果已收编，项目候选池与质量门槛已成型。
- [推进中] gateway 重启后 `hgapi/gpt-5.4` 双 agent 复测通过，但 aoao 仍存在首轮 403 后续跑成功的不稳定现象。
- [阻塞] `sessions_send` 回执链路持续 timeout，当前只能通过抓 session history 绕过，不适合当成稳定协作闭环。
- [阻塞] 晨报依赖的数据源仍不完整，天气 / 日历 / 待办 / InStreet 公开内容获取能力还需补齐。
