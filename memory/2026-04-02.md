# 2026-04-02 日记

> 补写时间：2026-04-03 21:27 CST
> 记录者：main agent cron（回看昨日会话与沉淀整理）

---

## 昨日任务汇总（2026-04-02）

### 完成事项

- **aoao 修复 #58560（Browser tools AJV regression）**：根因是 Zod 4 生成的 schema 使用 `draft-2020-12`，但 AJV 实例用的是 base 类只支持 draft-07。修复：将 `schema-validator.ts` 和 `llm-task-tool.ts` 改为使用 `Ajv2020`。测试全部通过（7+13 个用例）。PR 未提交（pre-existing build 问题 `failover-policy.js` 缺失，与本次修复无关）。
- **aoao 修复 #58556（Live model switch 阻断 fallback）**：根因是 `isSessionOverrideCurrent → !isSessionOverrideCurrent` 布尔值反转错误。修复后在 `src/agents/pi-embedded-runner/run.ts` 修正逻辑。无法完整测试（同一 pre-existing build 问题）。
- **aoao 修复 SSRF Telegram 问题**：`web-fetch.ts` 未传递 `ssrfPolicy`，导致 `api.telegram.org` 文件 CDN 被 SSRF 策略误拦截。修复：读取 `browser.ssrfPolicy.hostnameAllowlist` 并在 `runWebFetch` 时传入。commit 已提交。
- **aoao 修复 #58514（Google Chat Space/Group 消息被静默忽略）**：根因是 `spaceType !== "DM"` 误将 `GROUP_DM` 也当作 group，正确应为 `spaceType === "SPACE"`。修复后 commit 已提交。
- **xixi 完成 2026-04-02 12:11 UTC 扫描**：发现 3 个新 P1 regression issues（#59657、#59655、#59654），已更新 `OPENCLAW-PROJECT.md`。
- **晨briefing 已发送**（cron `morning-briefing` 09:08 CST 区域）：内容涵盖 #58560、#58556、#58561 三 P1 进展及阻塞事项。

### 进行中/阻塞事项

- **xixi → main 回传链路超时**：sessions_send 向 main 报告扫描结果仍存在 gateway timeout，链路可靠性待提升。
- **aoao 派单链路可达性**：历史问题，多 Agent 回传/派单链路超时，影响"xixi 发现 → main 转发 → aoao 执行"吞吐。
- **exec 审批策略收紧**（weiwei 会话确认）：weiwei 的 `exec` 命令现在需要手动审批（approval timeout 导致 deny），实际走 gateway 侧审批而非 sandbox，OpenClaw 升级后安全策略收紧是主因。
- **gh CLI 审批拦截**：多个 cron 的 gh CLI 命令因无预先审批而被拒，GitHub 方向扫描受限。

### 关键决策/结论

- **最高优先级新候选 #59655**：v2026.4.1 build artifact hash 不匹配导致所有 Feishu 消息完全失效，且 changelog 声称已修复"stable runtime alias files"——是 fix 的 regression，影响面极广。
- **次高优先级 #59657**：isolated cron model override 系统性回归，与历史 #57581/#57571/#57540 同类，应一次合并修复。
- **Feishu 群行为变更**：main 已修改 gateway 配置，以后只有 @ 才回复，不 @ 就潜水（20:26 CST 前后）。
- **InStreet 当前状态**：`instreet.coze.site` 正在"闭店装修"，无社区内容可抓，暂无可行动线索。
- **策略**：信息不完整时，采用"只记录已落盘、已追踪、已编号问题"保守记法，禁止编造。

### 可供晨报引用的 bullet

- 🔴 **P1 #59655**（Feishu v2026.4.1 regression）：build artifact hash 不匹配导致所有 Feishu 消息完全失效——changelog 声称修复的问题重现，需紧急修复
- 🔴 **P1 #59657**（isolated cron model override）：isolated cron job 切换不同 model 时直接 throw LiveSessionModelSwitchError，系统性回归，与 #57581/#57571/#57540 同根因
- 🟡 **P1 #59654**（`openclaw agents add` TypeError）：agents add 命令完全不可用，TypeError on undefined，阻断多 agent 创建
- ✅ **#58560** 已修复（Browser tools AJV）：改用 Ajv2020 支持 Zod 4 draft-2020-12 schema，测试通过，PR 待提交
- ✅ **#58556** 已修复（live model switch）：`isSessionOverrideCurrent` 布尔值修正，逻辑反转错误已纠正
- ✅ **SSRF Telegram** 已修复：web-fetch 现在正确传递 SSRF hostname allowlist，Telegram 文件 CDN 不再被误拦截
- ✅ **#58514** 已修复（Google Chat Space）：GROUP_DM 识别修正为 direct session，Space 消息不再被静默忽略
- ⚠️ **exec 审批策略收紧**：OpenClaw 升级后 gh CLI / find 等命令需要手动审批，cron 扫描受限；建议范总确认是否需要调整 exec allowlist

---

## 昨日其他事件（无特定分类）

- **weiwei 会话**：范总询问 OpenClaw 执行策略是否改变；确认是审批模式从免审变为需审。
- **workspace 收口 cron**（晚 20:26 CST）：workspace 目录无 temp/build/nested-git 问题，所有文件为预期 untracked，无需干预。

---

*本文件由 cron 每日自动创建并回填；如信息不足则基于保守总结，禁止编造。*
